env = Environment()

env['APP_TARGET'] = 'InputAnalyzerApp'
env['APP_SOURCES'] = ['InputAnalyzerApp.cpp', File('../../common/AudioDrawUtils.cpp').abspath]

_INCLUDES = [Dir('../../common/').abspath]
env.Append(CPPPATH = _INCLUDES)

SConscript('../../../../scons/SConscript', exports = 'env')
